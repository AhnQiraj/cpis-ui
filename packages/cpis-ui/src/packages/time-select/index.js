import CpisTimeSelect from './src/time-select.vue'

/* istanbul ignore next */
CpisTimeSelect.install = function (Vue) {
  Vue.component(CpisTimeSelect.name, CpisTimeSelect)
}

export default CpisTimeSelect

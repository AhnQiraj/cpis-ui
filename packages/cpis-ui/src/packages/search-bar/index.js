import CpisSearchBar from './src/search-bar.vue'

/* istanbul ignore next */
CpisSearchBar.install = function (Vue) {
  Vue.component(CpisSearchBar.name, CpisSearchBar)
}

export default CpisSearchBar
